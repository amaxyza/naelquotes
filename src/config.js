// Answer-time settings (in seconds). The player picks how long they get to enter
// the full sequence, via presets or the slider/input.
export const DEFAULT_SECONDS = 6
export const MIN_SECONDS = 2
export const MAX_SECONDS = 10
export const STEP_SECONDS = 0.5

export const TIME_PRESETS = [
  { label: 'Default', seconds: 6 },
  { label: 'Caller', seconds: 4 },
]
